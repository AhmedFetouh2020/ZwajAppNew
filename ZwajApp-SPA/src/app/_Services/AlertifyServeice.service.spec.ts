/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AlertifyServeiceService } from './AlertifyServeice.service';

describe('Service: AlertifyServeice', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AlertifyServeiceService]
    });
  });

  it('should ...', inject([AlertifyServeiceService], (service: AlertifyServeiceService) => {
    expect(service).toBeTruthy();
  }));
});
